
common:
  mkdirs:
    - /var/log/nginx/ribosome-examples/


configs:

  djangosite-gunicorn:
    includes:
      - static.conf
    certs:
      - site.crt
      - site.key

  flasksite-gunicorn:
    includes:
      - static.conf
    certs:
      - site.crt
      - site.key
